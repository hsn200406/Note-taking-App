<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= note.title %></title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >
</head>

<body>
<%- include('partials/header', { page: 'notes', user: user }) %>

  <div class="container mt-5">
    <h1 class="mb-3"><%= note.title %></h1>

    <div class="card">
      <div class="card-body">
        <p class="card-text" style="white-space: pre-wrap;">
          <%= note.content %>
        </p>
      </div>
    </div>

    <div class="mt-4 d-flex gap-2">
      <a href="/notes/<%= note._id %>/edit" class="btn btn-warning">
        Edit
      </a>
      <a href="/notes" class="btn btn-secondary">
        Back
      </a>
    </div>
  </div>
</body>
</html>
