<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link 
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" 
    rel="stylesheet"
  >
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Not-Taking App</h1>
    <h3 class="text-center mb-4 mt-5">Log In</h3>

    <div class="card card-body mx-auto" id = "login-form-container" style="max-width: 400px;">
      <form action="/auth/login" method="POST" id = "login-form">
        <div class="mb-3">
          <label class="form-label">Username</label>
          <input type="text" id = "login-username" placeholder="Enter your username here..." name="username" class="form-control" required>
        </div>
        <div class="mb-3">
          <label class="form-label">Password</label>
          <input type="password" id="login-password" placeholder="Enter your password here" name="password" class="form-control" required>
        </div>

        <div class="d-flex justify-content-between">
          <button type="submit" class="btn btn-success">Login</button>
          <a href="/auth/register" class="btn btn-secondary">Register</a>
        </div>
      </form>
      <div id="error-container">
        <% if (error) { %>
          <p style="color:red"><%= error %></p>
        <% } %>
      </div>
    </div>
  </div>
</body>
</html>
