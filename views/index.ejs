<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Note Taking App</title>

  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <%- include('partials/header', { page: 'notes', user: user }) %>

  <div class="container mt-5">

    <!-- Page Title -->
    <h1 class="text-center mb-4"><%= title %></h1>

    <!-- Search Bar -->
    <div class="search-container mb-4">
      <form id="searchForm" method="GET" action="/notes" class="search-form">
        <div class="search-input-wrapper">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="search-icon" viewBox="0 0 16 16">
            <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.003.004.009.01.01.01l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1 1 0 0 0-.01-.01zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z"/>
          </svg>
          <input 
            type="text" 
            name="search" 
            id="searchInput"
            class="search-input" 
            placeholder="Search notes by title or content..."
            value="<%= searchQuery || '' %>"
          >
          <% if (searchQuery) { %>
            <a href="/notes" class="search-clear-btn" title="Clear search">×</a>
          <% } %>
          <button type="submit" class="search-btn" title="Search">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="currentColor" viewBox="0 0 16 16">
              <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.003.004.009.01.01.01l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1 1 0 0 0-.01-.01zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z"/>
            </svg>
          </button>
        </div>
      </form>
    </div>

    <!-- Create Note Button -->
    <div class="d-flex justify-content-center mb-4">
      <a href="/notes/new" class="btn btn-success">Create a Note</a>
    </div>

    <!-- No notes message -->
    <% if (notes.length === 0) { %>
      <% if (searchQuery) { %>
        <p class="text-center text-muted">No notes found matching "<strong><%= searchQuery %></strong>". Try a different search term.</p>
      <% } else { %>
        <p class="text-center">No notes available. Add a note to get started!</p>
      <% } %>
    <% } %>

    <% if (notes.length > 0) { %>
      <% if (searchQuery) { %>
        <h3>Found <%= notes.length %> note<%= notes.length !== 1 ? 's' : '' %> matching "<%= searchQuery %>"</h3>
      <% } else { %>
        <h3>You have <%= notes.length %> notes</h3>
      <% } %>
    <% } %>

    <!-- Notes List -->
    <% notes.forEach(note => { %>
      <div class="card mb-3 shadow-sm">
        <div class="card-body">

        <!-- Note Content -->
        <p class="card-text" style="white-space: pre-wrap;"><%= note.title %></p>

        <!-- Created & Updated Dates -->
        <div class="d-flex flex-column align-items-end text-muted small mt-3">
          <span>
            Created: <%= new Date(note.createdAt).toLocaleString('en-US', {
              month: 'short',      // Jan, Feb
              day: '2-digit',      // 01, 02
              year: 'numeric',     // 2026
              hour: '2-digit',     // 14, 02
              minute: '2-digit',   // 30
              hour12: false        // 24-hour format; set true for AM/PM
            }) %>
          </span>

          <% if (note.updatedAt) { %>
            <span>
              Last Updated: <%= new Date(note.updatedAt).toLocaleString('en-US', {
                month: 'short',
                day: '2-digit',
                year: 'numeric',
                hour: '2-digit',
                minute: '2-digit',
                hour12: false
              }) %>
            </span>
          <% } %>
        </div>
          <!-- Action Buttons -->
          <div class="mt-3 d-flex gap-2">
            <a href="/notes/<%= note._id %>" class="btn btn-success btn-sm">View</a>
            <a href="/notes/<%= note._id %>/edit" class="btn btn-warning btn-sm">Edit</a>
            <form action="/notes/<%= note._id %>?_method=DELETE" method="POST" style="display:inline;">
              <button class="btn btn-danger btn-sm">Delete</button>
            </form>
          </div>

        </div>
      </div>
    <% }) %>

  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
