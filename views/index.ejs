<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Note Taking App</title>

  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <%- include('partials/header', { page: 'notes', user: user }) %>

  <div class="container mt-5">

    <!-- Page Title -->
    <h1 class="text-center mb-4"><%= title %></h1>

    <!-- Create Note Button -->
    <div class="d-flex justify-content-center mb-4">
      <a href="/notes/new" class="btn btn-success">Create a Note</a>
    </div>

    <!-- No notes message -->
    <% if (notes.length === 0) { %>
      <p class="text-center">No notes available. Add a note to get started!</p>
    <% } %>

    <h3>You have <%= notes.length %> notes</h3>

    <!-- Notes List -->
    <% notes.forEach(note => { %>
      <div class="card mb-3 shadow-sm">
        <div class="card-body">

        <!-- Note Content -->
        <p class="card-text" style="white-space: pre-wrap;"><%= note.title %></p>

        <!-- Created & Updated Dates -->
        <div class="d-flex flex-column align-items-end text-muted small mt-3">
          <span>
            Created: <%= new Date(note.createdAt).toLocaleString('en-US', {
              month: 'short',      // Jan, Feb
              day: '2-digit',      // 01, 02
              year: 'numeric',     // 2026
              hour: '2-digit',     // 14, 02
              minute: '2-digit',   // 30
              hour12: false        // 24-hour format; set true for AM/PM
            }) %>
          </span>

          <% if (note.updatedAt) { %>
            <span>
              Last Updated: <%= new Date(note.updatedAt).toLocaleString('en-US', {
                month: 'short',
                day: '2-digit',
                year: 'numeric',
                hour: '2-digit',
                minute: '2-digit',
                hour12: false
              }) %>
            </span>
          <% } %>
        </div>
          <!-- Action Buttons -->
          <div class="mt-3 d-flex gap-2">
            <a href="/notes/<%= note._id %>" class="btn btn-success btn-sm">View</a>
            <a href="/notes/<%= note._id %>/edit" class="btn btn-warning btn-sm">Edit</a>
            <form action="/notes/<%= note._id %>?_method=DELETE" method="POST" style="display:inline;">
              <button class="btn btn-danger btn-sm">Delete</button>
            </form>
          </div>

        </div>
      </div>
    <% }) %>

  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
